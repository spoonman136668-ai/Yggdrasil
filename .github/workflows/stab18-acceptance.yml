name: STAB-18 runner probe

on:
  push:
    branches:
      - validation/stab18-1e50388
  workflow_dispatch:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: echo RUNNER_OK_LINUX
  windows:
    runs-on: windows-latest
    steps:
      - run: echo RUNNER_OK_WINDOWS
  macos:
    runs-on: macos-latest
    steps:
      - run: echo RUNNER_OK_MACOS
