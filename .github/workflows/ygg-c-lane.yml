name: YGG-C Scaling Lane

on:
  push:
    branches:
      - research/ygg-c-scaling
    paths:
      - research/control/ygg-c-run.json
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ygg-c-research-lane
  cancel-in-progress: false

jobs:
  lane-run:
    runs-on: [self-hosted, Windows, X64, yggdrasil, ygg-c, research-only]
    timeout-minutes: 45

    steps:
      - name: Checkout exact lane commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
          persist-credentials: false
          clean: true

      - name: Restore bound LU2G loader bytes
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $Bound = 'research/applications/track-a/lu2g_second_task_compositional_service_transfer_v1.candidate.py'
          $Expected = '21d19cfe4aece4a067264c2ae80c4325ec77e964684ab3663fa285f0545880c8'
          git config core.autocrlf false
          if (Test-Path -LiteralPath $Bound) { Remove-Item -LiteralPath $Bound -Force }
          git checkout -- $Bound
          if ($LASTEXITCODE -ne 0) { throw 'BOUND_LOADER_RESTORE_FAILED' }
          $Actual = (Get-FileHash -Algorithm SHA256 -LiteralPath $Bound).Hash.ToLowerInvariant()
          if ($Actual -cne $Expected) { throw "BOUND_LOADER_HASH_MISMATCH expected=$Expected actual=$Actual" }

      - name: Assert lane isolation and read mode
        id: ctl
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:GITHUB_REPOSITORY -cne 'spoonman136668-ai/Yggdrasil') { throw "REPOSITORY_MISMATCH actual=$env:GITHUB_REPOSITORY" }
          if ($env:GITHUB_REF_NAME -cne 'research/ygg-c-scaling') { throw "BRANCH_MISMATCH actual=$env:GITHUB_REF_NAME" }
          $Raw = Get-Content -LiteralPath 'research/control/ygg-c-run.json' -Raw
          $Control = $Raw | ConvertFrom-Json
          if ($Control.schema -ne 1) { throw 'CONTROL_SCHEMA_MISMATCH' }
          if ($Control.lane -cne 'YGG-C') { throw "CONTROL_LANE_MISMATCH actual=$($Control.lane)" }
          if ($Control.mode -notin @('bootstrap','c1_benchmark','c2_parallel','c3_pressure','c4_topology','c5_transition','c6_factorial','c7_paired_arm')) { throw "CONTROL_MODE_UNSUPPORTED actual=$($Control.mode)" }
          if ($Raw -match '(?i)ckb-plane|ktrade|wingless|coinbase|broker|ygg-a|ygg-b') { throw 'LANE_ISOLATION_TOKEN_VIOLATION' }
          "mode=$($Control.mode)" >> $env:GITHUB_OUTPUT

      - name: Probe runner environment
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $OutDir = Join-Path $env:RUNNER_TEMP 'ygg-c-bootstrap'
          if (Test-Path -LiteralPath $OutDir) { Remove-Item -LiteralPath $OutDir -Recurse -Force }
          New-Item -ItemType Directory -Path $OutDir -Force | Out-Null
          $Nvidia = Get-Command nvidia-smi -ErrorAction SilentlyContinue
          [ordered]@{
            schema=1
            lane='YGG-C'
            mode='${{ steps.ctl.outputs.mode }}'
            github_sha=$env:GITHUB_SHA
            runner_name=$env:RUNNER_NAME
            identity=[Security.Principal.WindowsIdentity]::GetCurrent().Name
            powershell=$PSVersionTable.PSVersion.ToString()
            gpu=if ($Nvidia) { @(& nvidia-smi --query-gpu=name,memory.total,driver_version --format=csv,noheader 2>&1) } else { @() }
            git=(& git --version | Out-String).Trim()
            isolation='Yggdrasil YGG-C lane only'
          } | ConvertTo-Json -Depth 5 | Set-Content -LiteralPath (Join-Path $OutDir 'bootstrap.json') -Encoding UTF8

      - name: Provision portable Python runtime
        if: steps.ctl.outputs.mode == 'c1_benchmark' || steps.ctl.outputs.mode == 'c2_parallel' || steps.ctl.outputs.mode == 'c3_pressure' || steps.ctl.outputs.mode == 'c4_topology' || steps.ctl.outputs.mode == 'c5_transition' || steps.ctl.outputs.mode == 'c6_factorial' || steps.ctl.outputs.mode == 'c7_paired_arm'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $PyRoot = Join-Path $env:RUNNER_TEMP 'python312-ygg-c'
          $PyExe = Join-Path $PyRoot 'python.exe'
          if (-not (Test-Path -LiteralPath $PyExe)) {
              if (Test-Path -LiteralPath $PyRoot) { Remove-Item -LiteralPath $PyRoot -Recurse -Force }
              New-Item -ItemType Directory -Path $PyRoot -Force | Out-Null
              $Zip = Join-Path $env:RUNNER_TEMP 'python-3.12.10-embed-amd64.zip'
              Invoke-WebRequest -UseBasicParsing -Uri 'https://www.python.org/ftp/python/3.12.10/python-3.12.10-embed-amd64.zip' -OutFile $Zip
              Expand-Archive -LiteralPath $Zip -DestinationPath $PyRoot -Force
              $Pth = Join-Path $PyRoot 'python312._pth'
              @('python312.zip','.','Lib\\site-packages','import site') | Set-Content -LiteralPath $Pth -Encoding ASCII
              New-Item -ItemType Directory -Path (Join-Path $PyRoot 'Lib\\site-packages') -Force | Out-Null
              $GetPip = Join-Path $env:RUNNER_TEMP 'get-pip.py'
              Invoke-WebRequest -UseBasicParsing -Uri 'https://bootstrap.pypa.io/get-pip.py' -OutFile $GetPip
              & $PyExe $GetPip --disable-pip-version-check --no-warn-script-location
              if ($LASTEXITCODE -ne 0) { throw "GET_PIP_FAILED exit=$LASTEXITCODE" }
          }
          if (-not (Test-Path -LiteralPath $PyExe)) { throw "PYTHON_MISSING path=$PyExe" }
          $TrackA = Join-Path $env:GITHUB_WORKSPACE 'research\applications\track-a'
          $Pth = Join-Path $PyRoot 'python312._pth'
          @('python312.zip','.','Lib\site-packages',$TrackA,'import site') | Set-Content -LiteralPath $Pth -Encoding ASCII
          $PyRoot >> $env:GITHUB_PATH
          (Join-Path $PyRoot 'Scripts') >> $env:GITHUB_PATH
          & $PyExe --version
          & $PyExe -m pip --version

      - name: Install frozen C1 runtime
        if: steps.ctl.outputs.mode == 'c1_benchmark' || steps.ctl.outputs.mode == 'c2_parallel' || steps.ctl.outputs.mode == 'c3_pressure' || steps.ctl.outputs.mode == 'c4_topology' || steps.ctl.outputs.mode == 'c5_transition' || steps.ctl.outputs.mode == 'c6_factorial' || steps.ctl.outputs.mode == 'c7_paired_arm'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          python -m pip install --disable-pip-version-check --no-input numpy==2.3.5
          python -m pip install --disable-pip-version-check --no-input --index-url https://download.pytorch.org/whl/cpu torch==2.10.0
          python -c "import numpy,torch; print('numpy',numpy.__version__); print('torch',torch.__version__)"

      - name: Run YGG-C1 benchmark
        if: steps.ctl.outputs.mode == 'c1_benchmark' || steps.ctl.outputs.mode == 'c2_parallel'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $OutDir = Join-Path $env:RUNNER_TEMP 'ygg-c1'
          if (Test-Path -LiteralPath $OutDir) { Remove-Item -LiteralPath $OutDir -Recurse -Force }
          New-Item -ItemType Directory -Path $OutDir -Force | Out-Null
          python research/experiments/ygg-c/ygg_c1_consumer_cost_v1.py (Join-Path $OutDir 'c1-benchmark.json')
          Get-Content -LiteralPath (Join-Path $OutDir 'c1-benchmark.json')

      - name: Run YGG-C2 parallel throughput probe
        if: steps.ctl.outputs.mode == 'c2_parallel'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $OutDir = Join-Path $env:RUNNER_TEMP 'ygg-c2'
          if (Test-Path -LiteralPath $OutDir) { Remove-Item -LiteralPath $OutDir -Recurse -Force }
          New-Item -ItemType Directory -Path $OutDir -Force | Out-Null
          python research/experiments/ygg-c/ygg_c2_parallel_throughput_v1.py (Join-Path $OutDir 'c2-result.json')
          $R = Get-Content -LiteralPath (Join-Path $OutDir 'c2-result.json') -Raw | ConvertFrom-Json
          Get-Content -LiteralPath (Join-Path $OutDir 'c2-result.json')
          if (-not $R.valid) { throw 'YGG_C2_EVIDENCE_INTEGRITY_FAILED' }

      - name: Run YGG-C3 frozen resource-pressure retention probe
        if: steps.ctl.outputs.mode == 'c3_pressure'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $OutDir = Join-Path $env:RUNNER_TEMP 'ygg-c3'
          if (Test-Path -LiteralPath $OutDir) { Remove-Item -LiteralPath $OutDir -Recurse -Force }
          New-Item -ItemType Directory -Path $OutDir -Force | Out-Null
          python research/experiments/ygg-c/ygg_c3_resource_pressure_retention_v1.py (Join-Path $OutDir 'c3-result.json')
          $R = Get-Content -LiteralPath (Join-Path $OutDir 'c3-result.json') -Raw | ConvertFrom-Json
          Get-Content -LiteralPath (Join-Path $OutDir 'c3-result.json')
          if (-not $R.valid) { throw 'YGG_C3_EVIDENCE_INVALID' }
          if (-not $R.qualification.YGG_C3_DOUBLED_RESOURCE_PRESSURE_RETENTION) { throw 'YGG_C3_SCIENTIFIC_NEGATIVE' }

      - name: Run YGG-C4 frozen topology-sensitivity probe
        if: steps.ctl.outputs.mode == 'c4_topology'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $OutDir = Join-Path $env:RUNNER_TEMP 'ygg-c4'
          if (Test-Path -LiteralPath $OutDir) { Remove-Item -LiteralPath $OutDir -Recurse -Force }
          New-Item -ItemType Directory -Path $OutDir -Force | Out-Null
          python research/experiments/ygg-c/ygg_c4_topology_sensitivity_v1.py (Join-Path $OutDir 'c4-result.json')
          $R = Get-Content -LiteralPath (Join-Path $OutDir 'c4-result.json') -Raw | ConvertFrom-Json
          Get-Content -LiteralPath (Join-Path $OutDir 'c4-result.json')
          if (-not $R.valid) { throw 'YGG_C4_EVIDENCE_INVALID' }
          if (-not $R.qualification.YGG_C4_TOPOLOGY_SENSITIVITY) { throw 'YGG_C4_SCIENTIFIC_NEGATIVE' }

      - name: Run YGG-C5 frozen fine-pressure transition
        if: steps.ctl.outputs.mode == 'c5_transition'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $OutDir = Join-Path $env:RUNNER_TEMP 'ygg-c5'
          if (Test-Path -LiteralPath $OutDir) { Remove-Item -LiteralPath $OutDir -Recurse -Force }
          New-Item -ItemType Directory -Path $OutDir -Force | Out-Null
          python research/experiments/ygg-c/ygg_c5_fine_pressure_transition_v1.py (Join-Path $OutDir 'c5-result.json')
          $R = Get-Content -LiteralPath (Join-Path $OutDir 'c5-result.json') -Raw | ConvertFrom-Json
          Get-Content -LiteralPath (Join-Path $OutDir 'c5-result.json')
          if (-not $R.valid) { throw 'YGG_C5_EVIDENCE_INVALID' }

      - name: Run YGG-C6 frozen transition-cell factorial
        if: steps.ctl.outputs.mode == 'c6_factorial'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $OutDir = Join-Path $env:RUNNER_TEMP 'ygg-c6'
          if (Test-Path -LiteralPath $OutDir) { Remove-Item -LiteralPath $OutDir -Recurse -Force }
          New-Item -ItemType Directory -Path $OutDir -Force | Out-Null
          python research/experiments/ygg-c/ygg_c6_transition_cell_factorial_v1.py (Join-Path $OutDir 'c6-result.json')
          $R = Get-Content -LiteralPath (Join-Path $OutDir 'c6-result.json') -Raw | ConvertFrom-Json
          Get-Content -LiteralPath (Join-Path $OutDir 'c6-result.json')
          if (-not $R.valid) { throw 'YGG_C6_EVIDENCE_INVALID' }

      - name: Run YGG-C7 frozen paired-arm interaction origin
        if: steps.ctl.outputs.mode == 'c7_paired_arm'
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $ErrorActionPreference = 'Stop'
          $OutDir = Join-Path $env:RUNNER_TEMP 'ygg-c7'
          if (Test-Path -LiteralPath $OutDir) { Remove-Item -LiteralPath $OutDir -Recurse -Force }
          New-Item -ItemType Directory -Path $OutDir -Force | Out-Null
          python research/experiments/ygg-c/ygg_c7_paired_arm_interaction_origin_v1.py (Join-Path $OutDir 'c7-result.json')
          $R = Get-Content -LiteralPath (Join-Path $OutDir 'c7-result.json') -Raw | ConvertFrom-Json
          Get-Content -LiteralPath (Join-Path $OutDir 'c7-result.json')
          if (-not $R.valid) { throw 'YGG_C7_EVIDENCE_INVALID' }
          if (-not $R.qualification.YGG_C7_PAIRED_ARM_INTERACTION_ORIGIN) { throw 'YGG_C7_QUALIFICATION_FAILED' }

      - name: Upload lane evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ygg-c-${{ steps.ctl.outputs.mode }}-${{ github.run_id }}
          path: |
            ${{ runner.temp }}\ygg-c-bootstrap\**
            ${{ runner.temp }}\ygg-c1\**
            ${{ runner.temp }}\ygg-c2\**
            ${{ runner.temp }}\ygg-c3\**
            ${{ runner.temp }}\ygg-c4\**
            ${{ runner.temp }}\ygg-c5\**
            ${{ runner.temp }}\ygg-c6\**
            ${{ runner.temp }}\ygg-c7\**
          if-no-files-found: warn
          retention-days: 14
